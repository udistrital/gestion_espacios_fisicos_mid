package services_test

import (
	"testing"

	// "bou.ke/monkey"
	// "github.com/udistrital/espacios_fisicos_mid/services"
)

func TestPutActivarEspacioFisico(t *testing.T) {
//     t.Log("//////////////////////////////////")
//     t.Log("Inicio PutActivarEspacioFisico")
//     t.Log("//////////////////////////////////")

//     t.Run("Caso 1: Activacion exitosa de espacio fisico", func(t *testing.T) {
//         idEspacioFisico := 1810

// 		monkey.Patch(services.CambiarEspacioFisico, func(idEspacioFisico int, cambio bool, rollback bool){
// 		})
// 		defer monkey.Unpatch(services.CambiarEspacioFisico)

// 		monkey.Patch(services.CambiarAsignacionEspacioFisicoDependencia, func(idEspacioFisico int, cambio bool, rollback bool) {
// 		})
// 		defer monkey.Unpatch(services.CambiarAsignacionEspacioFisicoDependencia)

// 		monkey.Patch(services.CambiarTipoUsoEspacioFisico, func(idEspacioFisico int, cambio bool, rollback bool) {
// 		})
// 		defer monkey.Unpatch(services.CambiarTipoUsoEspacioFisico)

// 		monkey.Patch(services.CambiarCampos, func(idEspacioFisico int, cambio bool){
// 		})
// 		defer monkey.Unpatch(services.BusquedaNombre)


//         alerta, outputError := services.PutActivarEspacioFisico(idEspacioFisico)

//         if len(alerta) == 0 || alerta[0] != "Success" {
//             t.Errorf("Se esperaba una alerta con 'Success', pero se obtuvo: %v", alerta)
//         }

//         if outputError != nil {
//             t.Errorf("Se esperaba outputError nulo, pero se obtuvo: %v", outputError)
//         }

//         t.Log("Activacion de espacio fisico ejecutado exitosamente sin errores")
//     })
  
// }


// func TestPutDesactivarEspacioFisico(t *testing.T) {
//     t.Log("//////////////////////////////////")
//     t.Log("Inicio PutDesactivarEspacioFisico")
//     t.Log("//////////////////////////////////")

//     t.Run("Caso 1: Desactivacion exitosa de espacio fisico", func(t *testing.T) {
//         idEspacioFisico := 1810

// 		monkey.Patch(services.CambiarEspacioFisico, func(idEspacioFisico int, cambio bool, rollback bool){
// 		})
// 		defer monkey.Unpatch(services.CambiarEspacioFisico)

// 		monkey.Patch(services.CambiarAsignacionEspacioFisicoDependencia, func(idEspacioFisico int, cambio bool, rollback bool) {
// 		})
// 		defer monkey.Unpatch(services.CambiarAsignacionEspacioFisicoDependencia)

// 		monkey.Patch(services.CambiarTipoUsoEspacioFisico, func(idEspacioFisico int, cambio bool, rollback bool) {
// 		})
// 		defer monkey.Unpatch(services.CambiarTipoUsoEspacioFisico)

// 		monkey.Patch(services.CambiarCampos, func(idEspacioFisico int, cambio bool){
// 		})
// 		defer monkey.Unpatch(services.BusquedaNombre)


//         alerta, outputError := services.PutDesactivarEspacioFisico(idEspacioFisico)

//         if len(alerta) == 0 || alerta[0] != "Success" {
//             t.Errorf("Se esperaba una alerta con 'Success', pero se obtuvo: %v", alerta)
//         }

//         if outputError != nil {
//             t.Errorf("Se esperaba outputError nulo, pero se obtuvo: %v", outputError)
//         }

//         t.Log("Desactivacion de espacio fisico ejecutado exitosamente sin errores")
//     })
  
}